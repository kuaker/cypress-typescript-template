{
  "name": "applydigital",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:open": "npx cypress open",
    "cy:run": "npx cypress run",
    "cy:run:contact-us-tests": "cypress run --browser chrome --spec cypress/e2e/tests/contac-us-tests/*.ts",
    "cy:run:login-tests": "cypress run --browser chrome --spec cypress/e2e/tests/login-tests/*.ts",
    "cy:run:payment-tests": "cypress run --browser chrome --spec cypress/e2e/tests/payment-tests*.ts",
    "cy:run:product-tests": "cypress run --browser chrome --spec cypress/e2e/tests/payment-tests*.ts",
    "cy:qa": "npx cypress run --env version=qa",
    "cy:staging": "npx cypress run --env version=staging"
  },
  "keywords": [],
  "author": "Fernando J. Arroupe",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^13.10.0",
    "cypress-mochawesome-reporter": "^3.8.2",
    "prettier": "3.2.5",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "chance": "^1.1.11",
    "dotenv": "^16.4.5",
    "@badeball/cypress-cucumber-preprocessor": "latest",
    "@cypress/webpack-preprocessor": "latest",
    "ts-loader": "latest",
    "typescript": "latest" 
  }
}
